{
  "name": "sdcr-facebook-bot",
  "version": "1.0.0",
  "description": "Facebook groups monitor — scrapes the joined-groups feed and forwards posts to /api/facebook/capture",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "login": "node login.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright": "1.49.0"
  }
}
